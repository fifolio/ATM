{
  "buildCommand": "mkdir -p ~/.ssh && ssh-keyscan github.com >> ~/.ssh/known_hosts && git submodule update --init --recursive && npm run build",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD .",
  "outputDirectory": "dist"
}
